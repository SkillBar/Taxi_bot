# Исключаем только то, что не нужно ни WebApp, ни API (оба проекта из одного репо).
# api/ не исключаем — проект API (Root Directory: api) должен видеть папку api/
bot/
node_modules/
.env
.env.*
!.env.example
*.log
.DS_Store
