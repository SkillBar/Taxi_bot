# ========== Только для бота (локально или Railway) ==========
# Боту не нужны VITE_API_URL и DATABASE_URL — он общается с базой только через API.

# Токен вашего бота (from @BotFather)
BOT_TOKEN=8371812427:AAFGoYAorlBF2GN4jdypspq9L32AESiR69U

# Ссылка на ваш задеплоенный API (backend). Без слэша в конце.
API_URL=https://taxi-botapi.vercel.app

# Секретный ключ. Должен совпадать с API_SECRET в переменных API на Vercel.
API_SECRET=local-secret

# Ссылка на ваш задеплоенный WebApp (Mini App). Обязательно со слэшем в конце.
WEBAPP_URL=https://taxi-bot-rouge.vercel.app/

# ========== Остальное (API, WebApp build, БД) ==========

# API локально (для dev)
# API_URL=http://localhost:3001

# WebApp build: API base URL (Vite). Нужен только при сборке webapp на Vercel.
# Mini App в Telegram работает только по HTTPS — для локального теста используйте туннель (ngrok) и укажите его URL.
VITE_API_URL=http://localhost:3001
# Production: VITE_API_URL=https://taxi-botapi.vercel.app

# CORS (API): опционально. Если задан — разрешён только этот origin. Пусто = разрешены все (удобно для dev).
# WEBAPP_ORIGIN=https://taxi-bot-rouge.vercel.app

# Database (API)
DATABASE_URL=postgresql://user:password@localhost:5432/executor_bot
# For Neon: use pooled URL above; set DIRECT_URL to direct (non-pooler) URL for migrations.
# For local Postgres: can be same as DATABASE_URL or omit from schema.
DIRECT_URL=postgresql://user:password@localhost:5432/executor_bot

# Optional: external agent check (Yandex/CRM). If not set, mock is used.
# AGENT_CHECK_URL=https://your-crm.com/api/agents/check
# AGENT_CHECK_API_KEY=...

# Yandex Fleet API (driver-profiles/list). Для менеджеров и списка водителей.
# YANDEX_PARK_ID=...
# YANDEX_CLIENT_ID=...   # вида taxi/park/...
# YANDEX_API_KEY=...

# Optional: registration submit endpoint
# REGISTRATION_SUBMIT_URL=...
# REGISTRATION_SUBMIT_API_KEY=...
